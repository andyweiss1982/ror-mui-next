<%= react_component(
    'Signup',
    props:  {
              minimum_password_length:  @minimum_password_length,
              alert: {
                email:                  flash[:email],
                password:               flash[:password],
                password_confirmation:  flash[:password_confirmation]
              }
            }
) %>
