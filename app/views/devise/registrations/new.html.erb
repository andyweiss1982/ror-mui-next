<%= react_component(
    'Signup',
    props:  {
              minimum_password_length:  @minimum_password_length,
              alert: {
                email:                  flash[:email]&.join(', '),
                password:               flash[:password]&.join(', '),
                password_confirmation:  flash[:password_confirmation]&.join(', '),
              }
            }
) %>
